export interface Event {
    id: number,
    code: string,
    name: string,
    teams: number[]
}

export interface EventResponse {
    id: number,
    code: string,
    name: string,
    teams: number[]
}